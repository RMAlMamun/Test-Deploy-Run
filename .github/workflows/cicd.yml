name: CI/CD - Test, Artifact, Deploy (Self-Hosted EC2)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 18
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Frontend - install & build
        run: |
          set -e
          mkdir -p reports
          echo "=== TEST RUN $(date -u) ===" > reports/test-results.txt

          if [ -f frontend/package.json ]; then
            cd frontend
            npm ci
            npm run build
            echo "Frontend build: OK" >> ../reports/test-results.txt
            cd ..
          else
            echo "Frontend package.json not found - skipped" >> reports/test-results.txt
          fi

      - name: Backend - install & test
        run: |
          set -e
          if [ -f backend/package.json ]; then
            cd backend
            npm ci
            npm test || true
            echo "Backend tests: DONE" >> ../reports/test-results.txt
            cd ..
          else
            echo "Backend package.json not found - skipped" >> reports/test-results.txt
          fi

      - name: Upload test results artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: reports/test-results.txt
          retention-days: 7

  deploy:
    needs: test
    runs-on: [self-hosted, linux]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download test results artifact
        uses: actions/download-artifact@v4
        with:
          name: test-results
          path: downloaded-artifacts

      - name: Display artifact content
        run: |
          echo "===== Artifact Content ====="
          cat downloaded-artifacts/test-results.txt

      - name: Deploy to EC2 (3-tier)
        shell: bash
        run: |
          set -euo pipefail

          APP_DIR=/opt/Test-Deploy-Run
          sudo mkdir -p "$APP_DIR"
          sudo chown -R "$USER":"$USER" "$APP_DIR"

          rsync -a --delete ./ "$APP_DIR"
          cd "$APP_DIR"

          chmod +x IMPLEMENTATION_AUTO.sh
          printf "y\nbmidb\nbmi_user\123456\n" | sudo -E bash ./IMPLEMENTATION_AUTO.sh